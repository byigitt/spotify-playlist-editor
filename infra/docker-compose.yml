services:
  app:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    ports:
      - "${PORT:-3001}:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=${PORT:-3001}
      - HOST=0.0.0.0
    restart: unless-stopped
